(set include BOOL off .)
(set include TRUTH-VALUE on .)

(fmod 4 is
  sort S .
  op true : -> S .
  op or : S S -> S .
  vars x y : S .
  eq or(true,true) = true .
  eq or(x,y) = or(y,x) .
endfm)

(select tool MTT .)
(select external tool aprove .)
(check termination .)
