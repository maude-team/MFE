set print conceal off .
set trace off .
set trace select on .
trace select ---- $procTPDB
             ---- terminationCheck
             CoCo-load
             .

(cocoload .)
q

eof
(select CoCoModule .)
(select tool MTT .)
(select external tool aprove .)
(check termination .)
(select tool CRC .)
(check Church-Rosser .)
q
